# Default values for helm-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: dyne/tarantool-operator
  tag: 0.0.9-dyne
  pullPolicy: IfNotPresent
